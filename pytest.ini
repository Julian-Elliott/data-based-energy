[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Markers for selective testing
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    tunnel: marks tests that require SSH tunnel
    api: marks tests that require Home Assistant API
